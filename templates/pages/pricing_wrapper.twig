{% extends "layout.twig" %}

{% set title       = "fortrabbit pricing | " ~ stack_title ~ "' stack" %}
{% set activeClass = 'nav_pricing' %}
{% set link        = 'pricing' %}

{% if stack == 'uni' %}{# NOTE: PRO stack pricing is broken with scrolling, links to IDs #}
{% set bodyClass   = 'animscroll' %}
{% endif %}


{# TODO:
    Frank: @oli: update or remove "set CTA", i see you have updated the template but rechecked the following. I will remove all CTA when it becomes not clear how and where we can see some results from this and what the tracking is about at all.

    why is campaign set twiice?
#}


{% block precontent %}
    {% include 'snippets/stack-chooser.twig' %}
{% endblock %}


{% block content %}

    {% include innerHtml %}
    {% include "snippets/signup-cta.twig"  with { 'campaign': 'pricing' }%}

    {% include (stack == 'uni') ? "snippets/shared-snippets/app-aspects-uni.twig" : "snippets/shared-snippets/app-aspects-pro.twig" %}
    {% include "snippets/shared-snippets/platform-aspects.twig" %}

{% endblock %}


{% if stack == 'pro' %}
    {% set javascripts %}
    <script>
        $(function () { $('#preset-launch').trigger('click'); });
        </script>
    {% endset %}
{% endif %}
