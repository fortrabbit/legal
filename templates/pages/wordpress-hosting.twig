{% extends "layout.twig" %}

{% set title            = "WordPress hosting in the cloud" %}
{% set activeClass      = 'nav_laravel' %}
{% set bodyClass        = 'marketing' %}

{% set cta = {
    'campaign'        : 'wordpress',
    'button_wording_1': 'Start your trial',
    'button_wording_2': 'see WordPress hosting in action'
    }
%}


{% block precontent %}
<div class="hero image wordpress oblique-end">
    <p class="type-xxl m-top-l oblique-sub">
        professional
    </p>
    <h1 class="oblique-claim">
        WordPress hosting
    </h1>
    <p class="type-xxl m-top-l oblique-sub">
        with Git push-to-deploy
    </p>
</div>
{% endblock %}






{% block content %}

{% include 'snippets/quotes.twig' with {
    'quoteText'      : 'silentworks',
    'quoteClasses'   : 'hero-quote pull-up'
}%}

<article class="post m-top-xl">
    <p class="type-l">
        No need to introduce WordPress. It's a cornerstone of the web and one of the reasons for the popularity of PHP. It's a blogging engine as well as a CMS. It's hackable and extremely skinnable. It's not known for a very modern PHP. But hey, there is a huge community and clients really love it!
    </p>
    <p>
        fortrabbit is not the cheapest nor the most simplistic WordPress hosting service ever. It's just a modern hosting platform for you — the sophistcated PHP developer.
    </p>
</article>
<p class="type-xs m-top-m">
    WordPress is a trademark of the WordPress Foundation.
</p>


<h2 class="m-top-xl" id="apps">
    Featured WordPress Apps hosted on fortrabbit
</h2>

<div class="row gutter-l wrap m-top-m">

    {% set apps = getResource("customers-apps") %}
    {% for item in apps %}
    {% if item.technology == "WordPress" and item.single %}
    <div class="third half-t" data-category="{{item.category | lower}}">
        <a href="{{ item.url }}" class="browserchrome">
            <div class="addressbar clip">
                {{ item.url }}
            </div>
            <div class="screenshot" style="background-image:url('{{ assetsremote }}/img/{{ item.screenshotfile }}');"></div>
        </a>
        <div class="type-s m-top-s">
            <b>{{ item.name }}</b>: {{ item.info }}. 
            {% if item.technolgy %}
            Buit in {{ item.technology }}
            {% endif %}
        </div>
    </div>
    {% endif %}
    {% endfor %}
</div>

<p class="type-s m-top-m" style="z-index:1000; position: relative">
    <a href="/customers">See more from our customers</a>
</p>









<h2 class="m-top-xxl">WordPress hosting presets</h2>
{% include '/snippets/simple-presets.twig' with { campaign: 'laravel' }  %}

<div class="row wrap gutter-l m-top-xl collapse">
    <div class="quarter half-t column">
        <div class="alert edged">
            <h3>What matters</h3>
            <p>
                fortrabbit is not your traditional hosting. It's modern, automated and managed. Imagine it as serverless hosting where you can purely focus on your code instead of managing servers.
            </p>
        </div>
    </div>
    <div class="quarter half-t">
        {% include '/snippets/batteries-included.twig'  %}
    </div>
    <div class="quarter half-t">
        <h3>Documentation</h3>
        <p>
            <a href="{{ help_url }}/install-wordpress">Installing WordPress</a> on fortrabbit is straight forward. You deploy your code with <code>git push</code>.
        </p>
    </div>
    <div class="quarter half-t">
        {% include "snippets/why-not-navigation.twig" %}
    </div>
</div>
{% endblock %}
